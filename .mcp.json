{
  "mcpServers": {
    "n8n": {
      "command": "node",
      "args": ["mcp-n8n-server/index.js"],
      "env": {
        "N8N_WEBHOOK_URL": "${N8N_WEBHOOK_URL}",
        "N8N_API_KEY": "${N8N_API_KEY}"
      }
    }
  }
}
