# Server Configuration
SERVER_PORT=4100
NODE_ENV=development

# PostgreSQL Configuration (Direct connection to Supabase PostgreSQL)
PGHOST=127.0.0.1
PGPORT=7012
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=postgres

# Get these values from: cd apps/api && supabase status
# The observability server connects directly to PostgreSQL to access the observability schema
