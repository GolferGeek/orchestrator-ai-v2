# =============================================================================
# DOCKER PORT OVERRIDES - EXAMPLE TEMPLATE
# =============================================================================
# Copy this to .env.docker and customize for your environment.
#
# This file overrides the default development ports (6xxx) to Docker ports.
# docker-compose loads both .env and .env.docker, with .env.docker taking
# precedence for any overlapping variables.
#
# Port Scheme:
#   Local Dev:  6xxx (API: 6100, Web: 6101, LangGraph: 6200, etc.)
#   Docker:     7xxx (API: 7100, Web: 7101, LangGraph: 7200, etc.)
#   Production: 9xxx (API: 9100, Web: 9101, LangGraph: 9200, etc.)
#
# With this setup, you can run local dev AND Docker simultaneously!
# =============================================================================

# Core App Ports
API_PORT=7100
WEB_PORT=7101
VITE_WEB_PORT=7101
LANGGRAPH_PORT=7200

# Open Notebook Ports
OPEN_NOTEBOOK_FRONTEND_PORT=7201
OPEN_NOTEBOOK_API_PORT=7202
OPEN_NOTEBOOK_SURREALDB_PORT=7203

# Orch Flow Port
ORCH_FLOW_PORT=7102

# Observability Port (if needed)
SERVER_PORT=7300

# =============================================================================
# FRONTEND URL OVERRIDES
# =============================================================================
# These tell the frontend apps where to find the API when running in Docker.
# Update these to match your Docker port configuration.

VITE_API_BASE_URL=http://localhost:7100
VITE_API_NESTJS_BASE_URL=http://localhost:7100
VITE_API_URL=http://localhost:7100
VITE_HTTP_PORT=7101

# =============================================================================
# INFRASTRUCTURE SERVICES
# =============================================================================
# Ollama - Uses Docker container (not localhost) for Docker deployments
# Change to http://localhost:11434 if using Ollama installed on host machine
OLLAMA_BASE_URL=http://ollama:11434

# Supabase - Runs via CLI on host machine (not in Docker)
# Connect through host.docker.internal for Docker containers
SUPABASE_URL=http://host.docker.internal:6010
DATABASE_URL=postgresql://postgres:postgres@host.docker.internal:6012/postgres

# LangGraph database connection (uses individual vars, not DATABASE_URL)
DB_HOST=host.docker.internal
DB_PORT=6012
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres

# =============================================================================
# PRODUCTION EXAMPLE
# =============================================================================
# For Mac Studio production deployment, you might use:
#
# API_PORT=9100
# WEB_PORT=9101
# VITE_WEB_PORT=9101
# LANGGRAPH_PORT=9200
# OPEN_NOTEBOOK_FRONTEND_PORT=9201
# OPEN_NOTEBOOK_API_PORT=9202
# OPEN_NOTEBOOK_SURREALDB_PORT=9203
# ORCH_FLOW_PORT=9102
# SERVER_PORT=9300
#
# VITE_API_BASE_URL=https://api.yourdomain.com
# VITE_API_NESTJS_BASE_URL=https://api.yourdomain.com
# VITE_API_URL=https://api.yourdomain.com
